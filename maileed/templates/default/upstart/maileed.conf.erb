#!upstart
description "Maileed Stack"

pre-start script

bash << "EOF"
  mkdir -p /var/log/maileed
  chown -R <%= node['maileed']['deploy_user'] %> /var/log/maileed
EOF

end script

start on runlevel [2345]

stop on runlevel [016]
