check process unicorn with pidfile "<%= node[:deploy][:application][:deploy_to]%>/shared/pids/unicorn.pid"
  alert "suporte@mailee.me,pablo@panop.it" but not on { ppid }
  start program = "<%= node[:deploy][:application][:deploy_to] %>/shared/scripts/unicorn start" 
  stop program = "<%= node[:deploy][:application][:deploy_to] %>/shared/scripts/unicorn stop"
  group unicorn
