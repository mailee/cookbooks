check process unicorn with pidfile "<%= @deploy[:deploy_to]%>/shared/pids/unicorn.pid"
  start program = "<%= @deploy[:deploy_to] %>/shared/scripts/unicorn start" 
  stop program = "<%= @deploy[:deploy_to] %>/shared/scripts/unicorn stop"
  group unicorn
