check process unicorn with pidfile "<%= note[:deploy][:application][:deploy_to]%>/shared/pids/unicorn.pid"
  alert "suporte@mailee.me,pablo@panop.it" but not on { ppid }
  start program = "<%= note[:deploy][:application][:deploy_to] %>/shared/scripts/unicorn start" 
  stop program = "<%= note[:deploy][:application][:deploy_to] %>/shared/scripts/unicorn stop"
  group unicorn
